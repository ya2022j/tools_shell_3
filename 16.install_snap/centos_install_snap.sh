

yum install -y epel-release

yum install -y snapd

systemctl enable --now snapd.socket

ln -s /var/lib/snaped/snap  /snap


which snap

snap version 

sudo snap install another-redis-desktop-manager ;
sudo snap install hello-world
